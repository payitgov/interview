server:
  requestLog:
      appenders:
        - type: console
  applicationConnectors:
    - type: http
      port: 9000

# Logging settings.
logging:

  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO

  appenders:
    - type: console